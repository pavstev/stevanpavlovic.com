---
import type { HTMLAttributes } from "astro/types";

import { cn } from "../../lib/utils";

interface Props extends HTMLAttributes<"time"> {
  date: Date;
}

const { class: className, date, ...props } = Astro.props;
---

<time class:list={cn("tabular-nums", className)} datetime={date.toISOString()} {...props}>
  <slot>{date.toLocaleDateString()}</slot>
</time>
