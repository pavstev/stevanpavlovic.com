---
import type { HTMLAttributes } from "astro/types";

import { cva, type VariantProps } from "class-variance-authority";

import { cn } from "../../lib/cn";

const headingVariants = cva("font-display font-bold tracking-tight text-foreground selection:bg-primary/30", {
  defaultVariants: {
    size: "md",
    variant: "default",
  },
  variants: {
    size: {
      display: "text-4xl font-black sm:text-5xl md:text-6xl lg:text-7xl",
      lg: "text-2xl font-extrabold sm:text-3xl md:text-4xl",
      md: "text-lg font-bold sm:text-xl md:text-2xl",
      sm: "text-base font-bold sm:text-lg",
    },
    variant: {
      accent: "text-gradient-accent",
      default: "text-foreground",
      muted: "font-medium text-muted-foreground",
      primary: "text-gradient-primary",
    },
  },
});

export interface Props extends HTMLAttributes<"h1">, VariantProps<typeof headingVariants> {
  level?: 1 | 2 | 3 | 4 | 5 | 6;
}

const { class: className, level = 1, size, variant, ...props } = Astro.props;

const Tag = `h${level}` as any;
---

<Tag class:list={[cn(headingVariants({ size, variant }), className)]} {...props}>
  <slot />
</Tag>
