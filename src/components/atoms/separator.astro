---
import type { HTMLAttributes } from 'astro/types';

import { cn } from '../../lib/cn';

type Props = HTMLAttributes<'span'>;

const { class: className, ...props } = Astro.props;
---

<span class:list={cn("text-muted-foreground/50", className)} {...props}>•</span>
