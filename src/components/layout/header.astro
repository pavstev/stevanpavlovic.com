---
import { NAV_ITEMS, NAV_ITEMS_ARRAY } from "../../config";
import { isActivePath } from "../../lib/nav";
import Button from "../atoms/Button.astro";
import Container from "../atoms/Container.astro";
import Stack from "../atoms/Stack.astro";
import BrandIdentity from "../molecules/BrandIdentity.astro";
import LanguagePicker from "../molecules/LanguagePicker.astro";
import MobileMenuToggle from "../molecules/mobile-menu-toggle.astro";
import MobileMenu from "../molecules/mobile-menu.astro";

const { pathname } = Astro.url;
const isActive = (href: string) => isActivePath(pathname, href);
const isPost = pathname.startsWith("/blog/") && pathname.length > 6;
---

<header
  class="fixed top-4 z-50 w-full px-4 transition-all duration-500 ease-in-out md:px-0"
  id="site-header"
  transition:persist
>
  <Container>
    <Stack align="center" class="justify-center" direction="row">
      {/* Dynamic Island Container */}
      <div
        class="glass-strong hover:shadow-glow-subtle relative flex h-14 w-fit min-w-[300px] items-center rounded-full px-2 shadow-2xl transition-all duration-500 ease-[cubic-bezier(0.34,1.56,0.64,1)] hover:border-white/20"
        id="nav-island"
      >
        {/* Progress Background (for posts) */}
        {
          isPost && (
            <div
              class="absolute inset-0 -z-10 w-0 rounded-full bg-primary/5 transition-all duration-150"
              id="nav-progress-bg"
            />
          )
        }

        {/* Left: Brand */}
        <div class="shrink-0 pl-2">
          <BrandIdentity size="sm" variant="author" />
        </div>

        {/* Center: Desktop Nav */}
        <nav class="hidden items-center gap-1 px-4 md:flex" id="desktop-nav">
          {
            NAV_ITEMS_ARRAY.map((item) => {
              const active = isActive(item.href);
              return (
                <Button
                  aria-current={active ? "page" : undefined}
                  class:list={[
                    "relative rounded-full px-4 py-2 text-[13px] font-bold transition-all duration-300",
                    {
                      "bg-primary/10 text-primary": active,
                      "text-muted-foreground hover:bg-white/5 hover:text-foreground": !active,
                    },
                  ]}
                  data-nav-link
                  href={item.href}
                >
                  <span>{item.label}</span>
                  {active && (
                    <div
                      class="absolute -bottom-1 left-1/2 h-0.5 w-3 -translate-x-1/2 rounded-full bg-primary shadow-[0_0_10px_var(--color-primary)]"
                      transition:name="nav-indicator"
                    />
                  )}
                </Button>
              );
            })
          }
        </nav>

        {/* Right: Actions */}
        <div class="ml-auto flex items-center gap-2 pr-2">
          {/* Search Trigger */}
          <button
            aria-label="Search (Cmd+K)"
            class="flex size-9 items-center justify-center rounded-full text-muted-foreground transition-all hover:bg-white/5 hover:text-foreground active:scale-95"
            data-search-trigger
          >
            <svg class="size-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"
            ><circle cx="11" cy="11" r="8"></circle><path d="m21 21-4.3-4.3"></path></svg
            >
          </button>

          <div class="hidden md:block">
            <LanguagePicker />
          </div>

          <MobileMenuToggle size="sm" />
        </div>
      </div>
    </Stack>
  </Container>
</header>

<MobileMenu navItems={Object.values(NAV_ITEMS)} />

<script>
  import { initMobileMenu, updateActiveLinks } from "../../lib/mobile-menu";

  const init = () => {
    initMobileMenu();
    updateActiveLinks();

    const island = document.getElementById("nav-island");
    const progressBg = document.getElementById("nav-progress-bg");

    const handleScroll = () => {
      const scroll = window.scrollY;

      // Shrink header on scroll
      if (scroll > 50) {
        island?.classList.add("h-12", "scale-95", "shadow-glow-subtle");
        island?.classList.remove("h-14");
      } else {
        island?.classList.remove("h-12", "scale-95", "shadow-glow-subtle");
        island?.classList.add("h-14");
      }

      // Progress bar logic
      if (progressBg) {
        const winScroll = document.body.scrollTop || document.documentElement.scrollTop;
        const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
        const scrolled = (winScroll / height) * 100;
        progressBg.style.width = scrolled + "%";
      }
    };

    window.addEventListener("scroll", handleScroll);
    handleScroll();
  };

  init();
  document.addEventListener("astro:page-load", init);
</script>
