---
import LogoSvg from "@assets/logo.svg";
import { cn } from "@client/utils";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<LogoSvg aria-hidden="true" class={cn("", className)} height="20px" width="20px" />
