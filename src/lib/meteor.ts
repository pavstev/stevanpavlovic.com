export const initMeteors = (): void => {
  // Meteors are largely CSS-driven, but this function can be used for
  // additional JS-based interactivity or cleanup if needed.
  // Currently, the CSS animation handles the movement.
  // We export this to satisfy the import in meteor.astro.
  console.warn("Meteors initialized");
};
