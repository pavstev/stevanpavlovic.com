---
import type { GetStaticPaths } from 'astro';



export const getStaticPaths = (() => {
  return [
    { params: { collection: 'blog' } },
    { params: { collection: 'projects' } },
    { params: { collection: 'experience' } },
  ];
}) satisfies GetStaticPaths;

const { collection } = Astro.params;

const search = Astro.url.search;
console.log('Redirecting with search:', search);

if (collection === 'blog') {
  return Astro.redirect(`/blog/1/grid${search}`);
} else if (collection === 'projects') {
  return Astro.redirect(`/projects/1/grid${search}`);
} else if (collection === 'experience') {
  return Astro.redirect(`/experience/1/list${search}`);
}

return Astro.redirect(`/${search}`);
---
