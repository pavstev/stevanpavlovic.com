---
import { BackgroundPaths } from "@components/common/background-paths";
import { BentoGrid, type BentoItem } from "@components/kokonutui/bento-grid";
import PageLayout from "@components/layout/page-layout.astro";
import { Illustration } from "@components/ui/glowing-stars";
import { type CollectionEntry, getCollection } from "astro:content";

import { PROFILE } from "../config";

const featuredProjects: CollectionEntry<"projects">[] = await getCollection(
  "projects",
  ({ data }) => data.featured
);
const latestBlog: CollectionEntry<"blog">[] = (await getCollection("blog")).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
const experience: CollectionEntry<"experience">[] = (await getCollection("experience")).sort(
  (a, b) => b.data.startDate.valueOf() - a.data.startDate.valueOf()
);

const bentoItems: BentoItem[] = [
  {
    className: "col-span-1",
    description:
      "Architecting scalable backend systems with Node.js, Go, and Cloud-native technologies.",
    feature: "icons",
    href: "/about",
    id: "expertise",
    title: "Expertise",
  },

  {
    className: "col-span-2",
    description:
      "A showcase of enterprise-grade applications, AI agents, and high-performance platforms.",
    feature: "spotlight",
    href: "/projects",
    id: "works",
    spotlightItems: featuredProjects.slice(0, 5).map((p) => p.data.title),
    title: "Selected Works",
  },

  {
    className: "col-span-1",
    description: "Lead Backend Developer & System Architect with 10+ years of experience.",
    feature: "timeline",
    href: "/experience",
    id: "journey",
    timeline: experience.slice(0, 5).map((exp) => ({
      event: exp.data.role,
      year: exp.data.startDate.getFullYear().toString(),
    })),
    title: "Professional Journey",
  },

  {
    className: "col-span-1",
    description: "Diving deep into software architecture, devops, and the future of AI.",
    feature: "typing",
    href: "/blog",
    id: "insights",
    title: "Latest Insights",
    typingText: `// Latest Post: ${latestBlog[0]?.data.title || "Exploring Architecture"}\n\nprintf("Hello world! Reading: %s", blogTitle);`,
  },
];
---

<PageLayout
  description={PROFILE.bio}
  hideHeader={true}
  isFullWidth={true}
  slug="/"
  title={PROFILE.name}
>
  <BackgroundPaths client:load title={PROFILE.name} />

  <div class="relative overflow-hidden">
    <div class="pointer-events-none absolute inset-0 -z-10 bg-neutral-950">
      <Illustration className="h-full" client:load columns={30} mouseEnter={true} stars={300} />
    </div>
    <BentoGrid client:load items={bentoItems} />
  </div>
</PageLayout>
